---
name: triage
description: 推动 issue 和外部 PR 走过一组 triage 角色状态机 —— 分类、核实、必要时追问，并写出 agent 可直接执行的简报。当用户说"triage"、"分诊 issue"、"整理 issue"时触发。
disable-model-invocation: true
---

# Triage

推动项目 issue tracker 上的 issue 走过一个小型 triage 角色状态机。

如果本仓库把外部 pull request 也当作需求入口（见 issue tracker 配置），triage 同样覆盖它们：**PR 就是附带了代码的 issue** —— 同样的角色、同样的状态、同样的状态机，只有少量差异在下文以"对 PR 而言"标出。裸写的 `#42` 按 tracker 配置解析为 issue 或 PR。

triage 期间发到 issue tracker 上的每条评论或 issue **必须** 以这段声明开头：

```
> *This was generated by AI during triage.*
```

## 参考文档

- [AGENT-BRIEF.md](AGENT-BRIEF.md) —— 如何写可长期有效的 agent 简报
- [OUT-OF-SCOPE.md](OUT-OF-SCOPE.md) —— `.out-of-scope/` 知识库如何运作

## 角色

两个 **类别** 角色：

- `bug` —— 有东西坏了
- `enhancement` —— 新功能或改进

五个 **状态** 角色：

- `needs-triage` —— 等待维护者评估
- `needs-info` —— 等待报告者补充信息
- `ready-for-agent` —— 规格完整，可交给 AFK agent
- `ready-for-human` —— 需要人来实现
- `wontfix` —— 不会处理

对 PR 而言，同样的状态针对附带的代码来读：`ready-for-agent` 表示已附简报、agent 应在现有 diff 上继续推进；`ready-for-human` 表示已可交由人合并。

每个 triage 过的 issue 应恰好携带一个类别角色和一个状态角色。如果状态角色冲突，标记出来并在做任何事之前询问维护者。

这些是规范角色名 —— issue tracker 里实际使用的标签字符串可能不同。映射关系应该已经提供给你了 —— 如果没有，先运行 `tuanzii:setup-matt-pocock-skills`。

状态流转：未打标签的 issue 通常先进入 `needs-triage`；从那里流向 `needs-info`、`ready-for-agent`、`ready-for-human` 或 `wontfix`。报告者回复后，`needs-info` 回到 `needs-triage`。维护者随时可以推翻 —— 遇到看起来不寻常的流转，先标记并询问再继续。

## 调用方式

维护者调用 `/triage` 并用自然语言描述想做什么。理解意图并执行。例如：

- "给我看所有需要我关注的东西"
- "看看 #42"（issue 或 PR）
- "把 #42 移到 ready-for-agent"
- "哪些可以被 agent 认领了？"

## 展示需要关注的内容

查询 issue tracker，按 oldest first 呈现三个桶：

1. **未打标签** —— 从未 triage 过。
2. **`needs-triage`** —— 评估进行中。
3. **自上次 triage 笔记以来报告者有新动态的 `needs-info`** —— 需要重新评估。

PR 在范围内时，把外部 PR 也纳入这些桶，每行标注 `[PR]` 或 `[issue]`。发现环节只浮现 *外部* PR（tracker 配置定义谁算外部）—— 协作者进行中的 PR 不属于 triage 工作。这个过滤只作用于发现环节；被明确点名的 PR 无论作者是谁都要 triage。

展示数量和每项一行摘要，让维护者挑选。

## Triage 指定的 issue 或 PR

1. **收集上下文。** 读完整个 issue 或 PR（正文、评论、标签、作者、日期；对 PR 还要读 diff）。解析已有的 triage 笔记，避免重复询问已解决的问题。用项目领域词汇表探索代码库，尊重相关领域的 ADR。对代码库做两项检查：(a) **冗余** —— 按领域概念（而非请求的字面措辞）搜索请求的行为是否已有实现，并报告你查了哪些地方。如果找到了，就是"已实现"型 `wontfix`（第 5 步）。(b) **历史拒绝** —— 读 `.out-of-scope/*.md`，挑出与该请求相似的条目。

2. **给出建议。** 告诉维护者你的类别和状态建议及理由，外加一段与该请求相关的代码库简报 —— 包括该功能是否已实现。等待指示。

3. **核实声明。** 在任何追问之前，先验证声明是否成立。对 bug，按报告者的步骤复现。对 PR，确认 diff 做到了它声称的事 —— checkout 出来，跑相关测试或命令。报告结果：已确认（附代码路径）、复现失败，或细节不足（强烈的 `needs-info` 信号）。一次确认的核实能让 agent 简报质量大幅提升。

4. **追问（如需要）。** 如果请求还需要充实，一起运行 `tuanzii:grilling` 和 `tuanzii:domain-modeling` skill —— 一轮一轮地追问打磨，随着决策落地就地打磨领域术语并更新 `CONTEXT.md` / ADR。

5. **落地结果：**
   - `ready-for-agent` —— 发一条 agent 简报评论（[AGENT-BRIEF.md](AGENT-BRIEF.md)）。
   - `ready-for-human` —— 结构与 agent 简报相同，但要注明为什么不能委派给 agent（需要判断、需要外部访问权限、涉及设计决策、需要人工测试）。
   - `needs-info` —— 发 triage 笔记（模板见下）。
   - `wontfix` —— 关闭，评论内容取决于 *原因*：
     - **已实现** —— 该改动在代码库中已存在。指出它在哪里；**不要** 写入 `.out-of-scope/`（那个知识库是给 *被拒绝* 的请求用的，不是给已建成的功能用的）。
     - **拒绝（bug）** —— 礼貌解释，然后关闭。
     - **拒绝（enhancement）** —— 写入 `.out-of-scope/`，在评论中链接它，然后关闭（[OUT-OF-SCOPE.md](OUT-OF-SCOPE.md)）。
   - `needs-triage` —— 打上角色标签。有部分进展时可选择性评论。

## 快速状态覆盖

如果维护者说"把 #42 移到 ready-for-agent"，信任他们，直接应用角色。先确认你要做的事（角色变更、评论、关闭），然后执行。跳过追问。如果在没有追问环节的情况下移到 `ready-for-agent`，问一句他们是否想写一份 agent 简报。

## Needs-info 模板

```markdown
## Triage Notes

**What we've established so far:**

- point 1
- point 2

**What we still need from you (@reporter):**

- question 1
- question 2
```

把追问过程中解决的所有内容都收进 "established so far"，避免工作丢失。问题必须具体、可执行，而不是"请提供更多信息"。

## 恢复之前的会话

如果 issue 或 PR 上已有 triage 笔记，读它们，检查报告者是否回答了未决问题，呈现更新后的全貌再继续。不要重复询问已解决的问题。
