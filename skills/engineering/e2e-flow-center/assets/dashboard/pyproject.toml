[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "e2e-flow-center-dashboard"
version = "0.1.0"
description = "Temporary localhost dashboard bundled with the e2e-flow-center skill"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "PyYAML>=6,<7"
]
